Repo root: /home/ubuntu/.openclaw/workspace/prompt-clone-mini

Goal: Generate GPThost-pasteable React snippets for EVERY design analysis markdown in:
- /home/ubuntu/.openclaw/workspace/prompt-clone-mini/design analysis/lovable/*.md
- /home/ubuntu/.openclaw/workspace/prompt-clone-mini/design analysis/v0/*.md

TOOLING RULES (must follow):
- Use ONLY bash/view to read/list files.
- Use ONLY write_bash to write files (via heredocs). Do NOT use create/edit.
- Use ABSOLUTE PATHS everywhere.

Output directory:
- /home/ubuntu/.openclaw/workspace/prompt-clone-mini/gpthost_exports/snippets/

For each analysis markdown file:
- slug = filename without extension
- Write: /home/ubuntu/.openclaw/workspace/prompt-clone-mini/gpthost_exports/snippets/<slug>.html

SNIPPET FORMAT (GPThost expects a snippet, NOT a full HTML document):
Each <slug>.html file must contain ONLY:
- <div id="root"></div>
- <script src="https://cdn.tailwindcss.com"></script>
- React 18 UMD production + ReactDOM 18 UMD production
- Babel standalone
- <script type="text/babel"> ... </script>
Inside Babel script:
- Exactly ONE React functional component named in PascalCase derived from slug.
- Render it via ReactDOM.createRoot(document.getElementById('root')).render(<ComponentName />)
- No imports.
- Keep it lightweight: prefer gradients, CSS, SVG shapes, and small inline mock data.
- Match layout/typography/colors/sections described in the analysis as closely as practical.
- Make it responsive.

Also write:
1) /home/ubuntu/.openclaw/workspace/prompt-clone-mini/gpthost_exports/manifest.json
   - JSON array entries: { slug, title, websiteName, file }
   - title: use H1 from markdown if present else Title Case slug
   - websiteName: pcmini-<slug> (lowercase letters/digits/hyphens only), truncate to max 50 chars.
   - file: absolute snippet path.

2) /home/ubuntu/.openclaw/workspace/prompt-clone-mini/gpthost_exports/README.md
   - brief paste/deploy instructions.

Finally:
- Use bash to verify: count of .md analysis files == count of snippets generated.
- Print a short summary listing slugs generated.

Now execute.
